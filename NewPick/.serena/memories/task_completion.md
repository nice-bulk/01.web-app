# タスク完了時のチェックリスト

## 必須手順
1. **リント確認**: `pnpm lint` を実行してエラーがないことを確認
2. **型チェック**: `pnpm build` でTypeScriptのコンパイルエラーがないことを確認
3. **動作確認**: `pnpm dev` で開発サーバーを起動して目視確認

## 推奨手順
4. 変更内容を git でコミット

## 注意
- テストは現在未設定 (Vitestなど未導入)
- フォーマッターは未設定 (Prettierなど未導入)
