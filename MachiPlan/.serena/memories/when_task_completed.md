# タスク完了時にやること（MachiPlan）

1. **Lint チェック**: `pnpm lint` を実行してエラーがないか確認
2. **ビルド確認**: `pnpm build` を実行してビルドが通ることを確認
3. **動作確認**: `pnpm dev` で開発サーバーを起動して手動確認
4. **Git コミット**: 変更内容をコミット

## 注意
- 現在テストフレームワーク（Vitest 等）は未設定のため、テストコマンドはなし
- 型エラーは `tsc -b` で確認可能（`pnpm build` に含まれる）
